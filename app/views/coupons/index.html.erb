
  <div class="m-b-1">
    <%= link_to "可使用", user_coupons_notuse_path(current_user),remote:true,class:"nav-link bg-warning notuse filter p-x-1" %>|
    <%= link_to "已使用", user_coupons_used_path(current_user),remote:true,class:"nav-link used filter p-x-1"%>|
    <%= link_to "已過期", user_coupons_overdue_path(current_user),remote:true,class:"nav-link overdue filter p-x-1" %>
  </div>

  <div class="card-columns coupons">
    <% @coupons.each do |coupon| %>
      <%=render "coupons/coupon",coupon: coupon%>
    <%end%>
  </div>

            
      
