<%= raw @qrcode.as_html %>
    <style>
    table {
        border-width: 0;
        border-style: none;
        border-color: #0000ff;
        border-collapse: collapse;
    }
    
    td {
        border-left: solid 10px #000;
        padding: 0;
        margin: 0;
        width: 0px;
        height: 10px;
    }
    
    td.black {
        border-color: #000;
    }
    
    td.white {
        border-color: #fff;
    }
    </style>

 <%#= link_to user_coupon_path(@user,@coupon), method: :delete, class: 'btn btn-danger btn-sm', remote: true %>

 <%= link_to "兌換(只有店家才可以按的）",[@user,@coupon], :confirm => 'Are you sure?', :method => :delete  %>